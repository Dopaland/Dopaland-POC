# §19 Matrix Row Map

Maps this repository's contents to the 30 rows of the client's §19 sign-off matrix
(`docs/preregistration/D0PA1_PreRegistration_Clarifications_v0.7.pdf`, §19). That
numbering exists only in the client document; this file is what lets a session working
in this repository find, for any row, what (if anything) here evidences it.

**Status vocabulary**, derived from the repository, not from the client document's own
`OPEN` status column (which just means "not yet returned" — every row is `OPEN` there by
construction, since nothing has been sent back yet):

- `IMPLEMENTED` — code exists, is tested, and the capability works as built.
- `IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA` — the code exists and is tested against
  synthetic input; it has never touched a real recording or real session.
- `DEFINED-NOT-IMPLEMENTED` — the shape of the answer is described somewhere (a formula,
  a required return, a doc), but no code computes it.
- `BLOCKED-ON-CLIENT-DECISION` — cannot proceed until an external decision lands (D2's
  action space, the audio keep-or-remove call, sensor-swap hardware/scheduling).
- `NOT-APPLICABLE-IN-REPO` — the row's evidence is a collection-protocol or governance
  matter, not something that can live in this repository as code.

G1 applies to this document too: every status below is a factual description of what
exists. None of it is a claim that any threshold, decision rule, or verdict has been
applied — those remain for a human, after sign-off, against real data.

| # | Item | Module(s) / document(s) | Status | Notes |
|---|---|---|---|---|
| 1 | D1 feature separation | `features/{geometry,x_core,episodes,attention,audio,context}.py`; `tests/test_feature_separation.py`; `features/manifests/*.json`; `docs/D1_DEPENDENCY_MAP.md` | IMPLEMENTED | Dependency map, exclusion list, 5 manifests, and the lag-diagnostic's architectural counterpart (the test) all exist. The §2.3 lag diagnostic itself (a live statistical check on real X_core/A_t data) is not built — no real data exists to run it on. |
| 2 | D2 prediction target | none | BLOCKED-ON-CLIENT-DECISION | No code anywhere depends on or defines an action-class vocabulary. `schema/canonical_log_v1.json`'s `action_class` field is deliberately an open string, not an enum, for this reason. CLAUDE.md's BLOCKED section names this explicitly. |
| 3 | D3 reliability | `analysis/reliability.py`; `tests/test_reliability.py`; `docs/RELIABILITY.md` | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | `compute_sem`/`compute_rc`/`compute_bland_altman_*`/`compute_within_unit_cv` all implemented with bootstrap CIs. `compute_icc()` implements the exact repeated-unit distinction the client's §4.1 draws (units = scripted episodes, measurements = sessions) and *refuses* to run on a single-unit design — this directly answers the client's "return first: what is the repeated unit" instruction with a code-level guarantee rather than a promise. No real 3-session, matched-unit data exists yet. |
| 4 | D7 baseline | `analysis/baselines.py`; `tests/test_baselines.py`; `docs/D7_BASELINES.md` | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | All three representations (raw/session_z/persistent_z) computed on the same input. `B_person,t`/`S_person,t` both computed only from strictly-prior sessions (§4.2's leakage rule) — proven with a mutation test, not just documented (`docs/D7_BASELINES.md`'s leakage-test section). Session 1 emits `missing`/`no_prior_history` for persistent_z, never a fallback. `robust_mad`/`expanding` is the shipped default estimator/window rule; the client asked for a justified choice, not necessarily this one — still open for the vendor to state and defend in the response. |
| 5 | Primary metric / U | `simulation/models.py` (`neg_log_loss`); `simulation/precision.py` (`Metric`, `MACRO_F1_METRIC`, `NEG_LOG_LOSS_METRIC`); `docs/D6_SIMULATION.md` §11 | IMPLEMENTED | The oriented-utility convention (`U`, higher-is-better, `Δ = U(with) − U(without)`) is built exactly as the client's §5.1 specifies, and a real comparison (log loss vs. macro-F1, on synthetic data) exists showing log loss materially improves decidability. Applying this to the real M_core/M0b comparison is BLOCKED on D2. |
| 6 | Uncertainty | `simulation/precision.py` (`bootstrap_ci_on_delta`, `bootstrap_ci_on_delta_refit`); `tests/test_precision.py` | IMPLEMENTED, partially | Bootstrap CIs resampled at episode/session level (never trial), refit-per-replicate variant included, always reports CI on the *difference* not the two arms separately. **The client's §5.2 permutation test (its own pre-specified exchangeability unit, ≥1000 permutations, an actual null distribution / p-value) is NOT built anywhere** — `controls/time_shuffle.py` is explicitly a *diagnostic*, stamped `not_a_permutation_test_pvalue: True` in every result, and must not be read as satisfying this requirement. |
| 7 | Gate 3 rule | none (by design, G1) | DEFINED-NOT-IMPLEMENTED | The formula (`LB[U(M_core) − U(M0b)] > δ_Gate3`) is the client's own §5.3 text; no code computes it, correctly — there is no real M_core/M0b to compare (BLOCKED on D2) and no code may decide a verdict before sign-off (G1). `δ_Gate3` itself is unproposed. |
| 8 | Verdict rule | none (by design, G1) | DEFINED-NOT-IMPLEMENTED | Same reasoning as row 7, for `δ_attention`/`δ_audio`/`δ_latent` and the RETAIN/DROP/INCONCLUSIVE shape. `artefacts/precision_analysis_v2.md` applies this exact arithmetic shape to *illustrative* candidate δ values on simulated Δ — explicitly not a real verdict, stated as such throughout that document. |
| 9 | Leakage controls | `controls/leakage.py`; `tests/test_leakage.py`; `docs/CONTROLS.md` §3 | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | All four variants (valid/post_action/pre_action/timestamp_shift) built, pluggable trial source, negative control carried automatically. Confirmed on synthetic data that the mechanism detects an injected severe leak; the natural `post_action` variant's direction cannot be established on this generator at all (see `docs/CONTROLS.md` §3's task-conditional caveat) — only real trials can test that direction. "What counts as substantial for this task" (the client's own return item) is unproposed. |
| 10 | Split hygiene | `simulation/precision.py` (`chronological_split`, `build_features`) | IMPLEMENTED | Episode-respecting chronological split, train-only imputation mean and L2 model selection (never touches val/test data for fitting), nested temporally-ordered selection. Verified in `tests/test_precision.py`. |
| 11 | D4 reproducibility | `reproduce.py`, `Makefile`, `reproduce.ps1`, `compare_results.py`; `tests/test_reproduce.py`; `docs/D4_REPRODUCIBILITY.md` | IMPLEMENTED | Same-environment determinism verified bit-identical across two real runs (only `experiment_id`/`captured_at_utc` differ, by design). Cross-environment tolerance recommended (`1e-6` absolute) but not yet validated against a genuinely different machine. **The client's own acceptance test — Gargi running the command on a clean machine — has not happened**, and the confirmatory scope (real archived video + logged feature streams) does not exist yet; today's command regenerates the synthetic/self-contained analysis pipeline only. |
| 12 | D5 actual action | none | NOT-APPLICABLE-IN-REPO | Named funded-phase item by the client's own §8; nothing to build in this POC. CLAUDE.md's report-wording constraint (never imply on-screen action validates real-world action) is honoured by omission — no code anywhere makes that claim. |
| 13 | D6 precision simulation | `simulation/{generator,models,precision}.py` + 5 `run_*.py` drivers; `docs/D6_SIMULATION.md`; `artefacts/precision_analysis_v1.md`, `v2.md` | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | Serial dependence, learning, fatigue, class-frequency drift and bursty missingness all modelled (A1–A6). Stopping rule / feasibility decision is exactly the "escape hatch" the client's §9 describes: the worst-cell finding (session length × rare-class frequency) already shows some proposed δ thresholds are unreachable at this N — reported as arithmetic, not a recommendation. All synthetic; no real trial data exists to simulate *from* (the assumptions are invented and labelled as such throughout). |
| 14 | D8 attention validity | none | DEFINED-NOT-IMPLEMENTED | This is D0PA1's own Gate 2 (not the POC's, already-run Gate 2 — see CLAUDE.md's naming-collision table). No statistic, null distribution, minimum effect, N, uncertainty interval, or failure rule has been proposed anywhere in this repository. The existing V_so pitch-unreliability finding (see row 25 area / CLAUDE.md's "Attention / screen-orientation" section) is directly relevant background for whatever D8 criterion gets proposed, but is not itself a D8 answer. |
| 15 | Positive control (blink) | `controls/blink_positive.py`; `tests/test_blink_positive.py`; `docs/CONTROLS.md` §4 | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | Event-matching tolerance (`matching_tolerance_ms`, a real algorithm parameter) and three *proposed* criterion values (`criterion_event_f1=0.80`, `criterion_count_tolerance_fraction=0.20`, `criterion_count_min_clips_fraction=0.80`) are stored and hashed but never compared against anything in code (checked by parsing the module's own AST — `tests/test_blink_positive.py`'s `check_criterion_values_never_compared_in_code`). Verified against synthetic aperture streams through the real detector. **No real clip has been recorded.** |
| 16 | Null-input control | `controls/null_input.py`; `tests/test_controls.py`; `docs/CONTROLS.md` §1 | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | Event/excursion rule (`|z| ≥ threshold` sustained ≥ duration) and false-event-rate metric are both parameterized and hashed. **This control's pure-computation pieces (dispersion, excursion detection) are unit-tested with synthetic values; the control itself has never executed even once** — it needs a live webcam and a human operator for its full 10-minute run, which no session has provided. The client's own suggested reference ("null-input false-event rate should not exceed what the negative control produces") is not yet compared — both numbers would first need to exist. |
| 17 | Negative control | `controls/negative_control.py`; `tests/test_controls.py`; `docs/CONTROLS.md` §2 | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | Wired into every `compute_delta()` call unconditionally (no flag to disable), verified via an unaware-caller test. Extensively exercised — every D6 sweep and every leakage/time-shuffle run carries it — but always on synthetic data. |
| 18 | Time-shuffle | `controls/time_shuffle.py`; `tests/test_time_shuffle.py`; `docs/CONTROLS.md` §5 | IMPLEMENTED-BUT-NEVER-RUN-ON-REAL-DATA | Episode-order shuffle (never frame/trial), reuses `compute_delta`/`bootstrap_ci_on_delta` directly. Stamped `diagnostic_only: True, not_a_permutation_test_pvalue: True` on every result — see row 6's caveat about the client's separate, still-unbuilt inferential permutation test. |
| 19 | Modality ablation | none | BLOCKED-ON-CLIENT-DECISION | Requires `U_t` (audio) to exist at all — currently an intentionally empty stub pending the keep-or-formally-remove decision (CLAUDE.md BLOCKED section) — and D2 for `M_core` itself. Nothing to ablate against yet. |
| 20 | Context stress test | none | NOT-APPLICABLE-IN-REPO | Requires two real recorded sessions differing in lighting/camera placement — a collection-protocol item, not a code capability. No protocol document for it exists anywhere yet either. |
| 21 | Synthetic recovery | `simulation/latent_recovery.py`; `tests/test_latent_recovery.py`; `docs/D6_SIMULATION.md` §13 | IMPLEMENTED | Full Z_true → observation generator → EMA placeholder pipeline → recovery-error (Pearson r + standardised RMSE) machinery built, run, and reported on synthetic data with known ground truth — this is what row 21 actually asks for (validate the machinery before M2 touches real data), and it is done. The placeholder EMA is explicitly not the study's eventual latent model. |
| 22 | Sensor swap | none | BLOCKED-ON-CLIENT-DECISION | Pending second-camera hardware and an FPS feasibility test (CLAUDE.md BLOCKED section). Must happen *during* real collection or the opportunity is permanently lost — cannot be built or rehearsed in advance without the hardware. |
| 23 | Audio acquisition | `features/audio.py` (intentionally empty stub) | BLOCKED-ON-CLIENT-DECISION | No microphone capture, no clock-sync mechanism, no audio processing anywhere in this repository. Pending the keep-or-formally-remove decision. |
| 24 | LLM read | `stage2_personality_agent.py`; `docs/PRIVACY_EVIDENCE.md` §4 (a real logged exchange, verbatim) | IMPLEMENTED | One-directional call (`Features → model → LLM interpretation → TERMINAL`) by construction — the agent module has no write path back into any feature/calibration/model code, confirmed by reading its own call graph (it is called at the end of the window-processing cycle and its return value is only ever logged/displayed, never passed back into `features.x_core`/`features.episodes`). No dedicated test asserts this structurally the way `tests/test_feature_separation.py` does for A_t/U_t; that would be a reasonable follow-on hardening. |
| 25 | V_es / V_pd wording | CLAUDE.md's HONEST-FRAMING WORDING section; `docs/PRIVACY_EVIDENCE.md` §4 | IMPLEMENTED | The exact required phrase ("implemented; pilot-scale candidate signals; not validated as psychological constructs") is codified in CLAUDE.md and shown in actual use in a real logged agent prompt. |
| 26 | A verification | `docs/AUDIT_A_COLUMN.md` | IMPLEMENTED | The 11-item table the client's §13 asks for, re-checked against committed code, with commit hashes and artefacts once version control existed. Result on record: 9 verified, 2 partial (blink/gaze — code fixed, but the only logged artefact predates the fix). |
| 27 | Repository / provenance | `PROVENANCE.md`; `.gitignore`; `.githooks/pre-commit`; `manifest/data_manifest.csv`; `logs/variant_log.jsonl`; `docs/GATE0_PROVENANCE.md`; `docs/preregistration/` | IMPLEMENTED, partially | No raw data in history (verified by content-scan, not just extension), manifest committed, variant log running. **The genuine pre-registration response is now committed** (`docs/preregistration/D0PA1_Section19_SignOff_Response.docx`, committed unmodified this task, superseding an earlier session's agent-authored substitute that was found to be improper and has been removed from version control — see `docs/preregistration/README.md`). **Not yet done**: a tagged frozen release (no git tag exists in this repository yet — correctly, since confirmatory collection hasn't started) and genuine held-out-data storage separation, which the response's own §4.27 and `PROVENANCE.md`'s escape-hatch language both already record as a stated limitation for a single developer rather than a false claim of a control that doesn't exist. |
| 28 | Canonical schema | `schema/canonical_log_v1.json`, `schema/canonical_log_writer.py`; `docs/CANONICAL_LOG_SCHEMA.md` | IMPLEMENTED | Versioned (`schema_version: "2"`), includes `context_id`/`device_id` as required fields per the client's own row-28 note, hand-validated at write time. Not yet wired into any live capture loop — a real capture session does not write through this schema today. |
| 29 | Stopping / exclusions | `docs/STOPPING_AND_EXCLUSION_RULES.md`; `docs/preregistration/D0PA1_Section19_SignOff_Response.docx` §4.29 | DEFINED-NOT-IMPLEMENTED | **Corrected from an earlier version of this row**, which concluded no rule content existed anywhere — that check missed the vendor's actual §4.29 response (not yet located on this machine at the time). All eight required categories now have proposed rule content, transcribed directly from §4.29 into `docs/STOPPING_AND_EXCLUSION_RULES.md` (seven under their own named heading; the eighth, general trial/session exclusion, covered by §4.29's closing binding principle rather than a separately-titled rule — noted as such, not silently merged). Status is `DEFINED-NOT-IMPLEMENTED`, not `IMPLEMENTED`, for two reasons: the rules are **proposed** ("Status: RETURNED... Nothing in this document is confirmed by appearing in it"), not yet client-signed, and no code in this repository enforces any of them yet (correctly — implementing an exclusion rule before sign-off would apply a rule not yet agreed). |
| 30 | Privacy / retention | `simulation/retention.py`, `docs/PRIVACY_AND_RETENTION.md` (this task) | IMPLEMENTED, partially | Retention/deletion mechanism (config-hashed period + location, deletion log, dry-run default) built this task — see `docs/PRIVACY_AND_RETENTION.md`. **The actual storage location is explicitly not decided** (`logs/` sits inside this repository's directory, untracked; `PROVENANCE.md`'s own open item) — the mechanism takes it as a parameter rather than assuming an answer. |
